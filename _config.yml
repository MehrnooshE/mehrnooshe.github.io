title: "Bits, Bytes & Base Pairs"
author: "Mehrnoosh Emamifar"
description: "From circuits to cells: documenting my journey from computer engineering to bioinformatics."

linkedin_username: "linkedin.com/in/mehrnoosh-emamifar"

# No theme — using custom layouts
# theme: "minima"

kramdown:
  syntax_highlighter_opts:
    disable: true

plugins:
  - jekyll-feed
  - jekyll-sitemap

titles_from_headings:
  strip_title: true
  collections: true

defaults:
  - scope:
      path: ""
      type: post
    values:
      layout: post
      tags: Other
